<div class="container my-5">
  <div class="row">
    <div class="col-sm-12 px-sm-0">
      <h2>商品一覧(全<%= @items_all.count %>件)</h2>
    </div>
  </div>

  <div class="row mt-3 mb-3 text-center text-md-left">
    <% @items.each do |item| %>
    <div class="col-xs-10 col-sm-10 col-md-6 col-lg-3">
      <div>
        <%= link_to item_path(item) do %>
          <p class="img-wrap"><%= attachment_image_tag item, :image, size: "200x200", fallback: "no_image.jpg" %></p>
        <% end %>
        <div class="card-body">
          <p class="card-text"><%= item.name %></p>
          <% if item.is_active == true %>
            <p class="card-text"><%= (item.price * 1.1).floor %>円(税抜き)</p>
          <% else %>
            <p class="text-danger font-weight-bold card-text">販売停止中</p>
          <% end %>
          </div>
      </div>
    </div>
    <% end %>
  </div>
</div>